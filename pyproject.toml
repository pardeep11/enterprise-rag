[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "enterprise-rag"
version = "0.1.0"
dependencies = [
    "bm25s",
    "ranx",
    "fastapi",
]

[tool.setuptools.packages.find]
where = ["."]
include = ["app*"]